const statusDiv = document.querySelector('.status-actions');
const resetDiv = document.querySelector('.reset');
const cellDivs = document.querySelectorAll('.grid-cell');
const nextPlayer = document.querySelector('.next-player');

export {statusDiv, resetDiv, cellDivs, nextPlayer};